[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=src uvicorn kwabo.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/api/health"
healthcheckTimeout = 30
